version: '3.8'

services:
  bot:
    build: .
    container_name: telegram_review_bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_ID=${ADMIN_ID}
    env_file:
      - .env

